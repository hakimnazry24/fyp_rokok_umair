export type RecordType = {
  id: string;
  dateTime: Date;
  product: string;
  customerAge: number;
  retailer: string;
  created_at: Date;
};

export type CigaretteType = {
  id: string;
  name: string;
  manufactured_date: Date;
  factory: string;
};
